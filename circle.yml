dependencies:
  override:
    - sudo apt-get install davfs2
    - go get -v github.com/spf13/hugo

test:
  override:
    - hugo

deployment:
  neocities:
    branch: master
    commands:
      - ./deploy.sh
